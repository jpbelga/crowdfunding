// postcss.config.js
module.exports = {
  plugins: {
    'tailwindcss': {}, // Para Tailwind CSS v3.x, referenciamos o pacote 'tailwindcss' diretamente.
    'autoprefixer': {},
    // Outros plugins PostCSS, se você os tiver, viriam aqui.
  }
};
